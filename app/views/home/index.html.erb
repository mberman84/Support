<h1>Home#index</h1>
<ul>
<% @issues.each do |issue| %>
	<li><%= link_to issue.subject, app_issue_path(issue.app, issue) %> - 
		(Comments - <%= issue.comments.count %>) -
		(Votes - <%= issue.voterships.count %>)</li>
<% end %>
</ul>

<%= link_to "Home", root_path %> | 
<%= link_to "Apps", apps_path %> |
<%= link_to "Issues", issues_path %>
